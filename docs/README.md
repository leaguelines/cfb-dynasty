# Docs

This repository is the **dynasty save-file library** (parse + export).

Gameplay reverse-engineering notes, FranTk/AttribSys extracts, MMC/Frosty helpers, and Ghidra work live in the sibling project:

`~/Documents/Projects/cfb27-re`

For local schema/tuning tests, `data/cfb27-db-data` may be a symlink into that project (gitignored under `/data/`).
